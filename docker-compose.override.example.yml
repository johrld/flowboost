# Local overrides for docker-compose.yml
# Copy to docker-compose.override.yml (gitignored) and uncomment what you need.

services:
  api:
    volumes:
      # ── Claude CLI Credentials (Max subscription) ──────────
      # Only needed if you use Option 3 (CLI Credentials) from README.
      # Requires Claude Code CLI installed and logged in on the host.
      #
      # On macOS, export credentials from Keychain first:
      #   security find-generic-password -s "Claude Code-credentials" -w > ~/.claude/.credentials.json
      # Export BEFORE the first `docker compose up`: if the file is missing,
      # Docker creates an empty directory at that path, which then breaks the export.
      # On Linux, the file already exists after `claude login`.
      #
      # - ~/.claude.json:/root/.claude.json:ro
      # - ~/.claude/.credentials.json:/root/.claude/.credentials.json:ro
    environment: {}
      # ── Custom domain (e.g. Cloudflare Tunnel) ─────────────
      # - FRONTEND_URL=https://app.yourdomain.com

  dashboard:
    environment: {}
      # ── Custom API URL ─────────────────────────────────────
      # - NEXT_PUBLIC_API_URL=https://api.yourdomain.com
