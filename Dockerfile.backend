FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --production=false

COPY . .
RUN npm run build

RUN npm prune --production

EXPOSE 6000
CMD ["node", "--env-file=.env", "dist/index.js"]
